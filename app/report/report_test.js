'use strict';

describe('baltcoGo.report module', function() {

  beforeEach(module('baltcoGo.report'));

  describe('report controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var rprtCtrl = $controller('ReportCtrl');
      expect(rprtCtrl).toBeDefined();
    }));

  });
});